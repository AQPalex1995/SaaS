# Dependencies
node_modules/
server/node_modules/

# Environment
.env
.env.local
server/.env

# Build outputs
dist/
server/dist/

# SQLite and local storage (data/ contains browser profile, credentials, cache — NEVER commit)
data/*
!data/.gitkeep

# Dev scratch scripts
scratch/

# Drizzle Kit generated migrations cache/meta if desired (keep migrations tracked if committed, but can ignore temporary files)
# server/drizzle/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db